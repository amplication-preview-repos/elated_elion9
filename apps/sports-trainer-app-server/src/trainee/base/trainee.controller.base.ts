/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { TraineeService } from "../trainee.service";
import { TraineeCreateInput } from "./TraineeCreateInput";
import { Trainee } from "./Trainee";
import { TraineeFindManyArgs } from "./TraineeFindManyArgs";
import { TraineeWhereUniqueInput } from "./TraineeWhereUniqueInput";
import { TraineeUpdateInput } from "./TraineeUpdateInput";
import { TrainingSessionFindManyArgs } from "../../trainingSession/base/TrainingSessionFindManyArgs";
import { TrainingSession } from "../../trainingSession/base/TrainingSession";
import { TrainingSessionWhereUniqueInput } from "../../trainingSession/base/TrainingSessionWhereUniqueInput";

export class TraineeControllerBase {
  constructor(protected readonly service: TraineeService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Trainee })
  async createTrainee(
    @common.Body() data: TraineeCreateInput
  ): Promise<Trainee> {
    return await this.service.createTrainee({
      data: data,
      select: {
        contactDetails: true,
        createdAt: true,
        fitnessGoals: true,
        id: true,
        name: true,
        updatedAt: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [Trainee] })
  @ApiNestedQuery(TraineeFindManyArgs)
  async trainees(@common.Req() request: Request): Promise<Trainee[]> {
    const args = plainToClass(TraineeFindManyArgs, request.query);
    return this.service.trainees({
      ...args,
      select: {
        contactDetails: true,
        createdAt: true,
        fitnessGoals: true,
        id: true,
        name: true,
        updatedAt: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Trainee })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async trainee(
    @common.Param() params: TraineeWhereUniqueInput
  ): Promise<Trainee | null> {
    const result = await this.service.trainee({
      where: params,
      select: {
        contactDetails: true,
        createdAt: true,
        fitnessGoals: true,
        id: true,
        name: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Trainee })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async updateTrainee(
    @common.Param() params: TraineeWhereUniqueInput,
    @common.Body() data: TraineeUpdateInput
  ): Promise<Trainee | null> {
    try {
      return await this.service.updateTrainee({
        where: params,
        data: data,
        select: {
          contactDetails: true,
          createdAt: true,
          fitnessGoals: true,
          id: true,
          name: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Trainee })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async deleteTrainee(
    @common.Param() params: TraineeWhereUniqueInput
  ): Promise<Trainee | null> {
    try {
      return await this.service.deleteTrainee({
        where: params,
        select: {
          contactDetails: true,
          createdAt: true,
          fitnessGoals: true,
          id: true,
          name: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Get("/:id/trainingSessions")
  @ApiNestedQuery(TrainingSessionFindManyArgs)
  async findTrainingSessions(
    @common.Req() request: Request,
    @common.Param() params: TraineeWhereUniqueInput
  ): Promise<TrainingSession[]> {
    const query = plainToClass(TrainingSessionFindManyArgs, request.query);
    const results = await this.service.findTrainingSessions(params.id, {
      ...query,
      select: {
        createdAt: true,
        dateTime: true,
        id: true,

        trainee: {
          select: {
            id: true,
          },
        },

        trainer: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
        venue: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/trainingSessions")
  async connectTrainingSessions(
    @common.Param() params: TraineeWhereUniqueInput,
    @common.Body() body: TrainingSessionWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      trainingSessions: {
        connect: body,
      },
    };
    await this.service.updateTrainee({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/trainingSessions")
  async updateTrainingSessions(
    @common.Param() params: TraineeWhereUniqueInput,
    @common.Body() body: TrainingSessionWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      trainingSessions: {
        set: body,
      },
    };
    await this.service.updateTrainee({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/trainingSessions")
  async disconnectTrainingSessions(
    @common.Param() params: TraineeWhereUniqueInput,
    @common.Body() body: TrainingSessionWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      trainingSessions: {
        disconnect: body,
      },
    };
    await this.service.updateTrainee({
      where: params,
      data,
      select: { id: true },
    });
  }
}
